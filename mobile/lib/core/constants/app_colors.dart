import 'package:flutter/material.dart';

class AppColors {
  // Arka Plan Renkleri (Zinc Dark Theme)
  static const Color background = Color(0xFF09090B); // Zinc 950
  static const Color surface = Color(0xFF18181B);    // Zinc 900
  static const Color surfaceLight = Color(0xFF27272A); // Zinc 800
  static const Color card = Color(0xFF141417);
  static const Color cardBorder = Color(0xFF2E2E33);

  // Vurgu & Marka Rengi (Amber / Altın)
  static const Color primary = Color(0xFFF59E0B);    // Amber 500
  static const Color primaryLight = Color(0xFFFBBF24); // Amber 400
  static const Color primaryDark = Color(0xFFD97706);  // Amber 600
  static const Color primaryGlow = Color(0x33F59E0B);

  // Metin Renkleri
  static const Color textPrimary = Color(0xFFF4F4F5);   // Zinc 100
  static const Color textSecondary = Color(0xFFA1A1AA); // Zinc 400
  static const Color textMuted = Color(0xFF71717A);     // Zinc 500

  // Durum Renkleri
  static const Color success = Color(0xFF10B981); // Emerald 500
  static const Color danger = Color(0xFFEF4444);  // Red 500
  static const Color warning = Color(0xFFF59E0B); // Amber 500
  static const Color info = Color(0xFF06B6D4);    // Cyan 500
  static const Color purple = Color(0xFF8B5CF6);  // Purple 500

  // Masa Durum Renkleri
  static const Color tableEmpty = Color(0xFF27272A);
  static const Color tableOccupied = Color(0xFFD97706);
  static const Color tableBillRequested = Color(0xFF0284C7);
}
